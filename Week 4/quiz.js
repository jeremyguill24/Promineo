const customers = [
    {
        name: 'Sam',
        address: {
            street: '1234 W Bell Rd',
            city: 'Phoenix',
            zip: '85308',
            state: 'AZ'
        },
        membershipLevel: 'GOLD',
        age: 32
    }]

let findShit = customers.find(customers => customers.name === 'C'); {
    if (customers.city != 'Peoria')

        console.log(customers.name);

}

console.log(findShit);

